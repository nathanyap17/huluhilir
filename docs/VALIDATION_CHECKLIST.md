# VALIDATION_CHECKLIST.md

> ⚠️ **Migration status:** items below marked **✅ CONFIRMED (v1 build)** are proven — the real v1 build validated them, not just planned to. Items marked **🔄 v2 NEW** are additional checks the v2 changes require. Items marked **⛔ OBSOLETE (v2)** applied only to AICC's constraints and no longer matter for AgroHack — don't waste time re-verifying them.
>
> **Experiments run in `../sandbox/EXPERIMENTS.md`**, deliberately outside this repo.
> This file tracks *status and consequences* — the runnable code lives there.
> Update this after each sandbox session.

---

## Status board

| EXP | Item | Owner | Status | Consequence if failed |
|---|---|---|---|---|
| **1** | ⛔ OBSOLETE (v2) — pre-build ruling from organisers | Zoe | — | AICC-only question. AgroHack explicitly allows pre-building — nothing to resolve here |
| **2** | MMS Iban TTS exists + intelligible | Nathan | ☐ | MVP pre-recorded clips only; amend Iban claims |
| **3** | Ollama reachable from phone over hotspot | Nathan | 🟡 | Ollama server confirmed reachable and responsive on `localhost:11434` this session; **phone-over-hotspot leg not tested** (no physical device in this environment) — still needs a real-device check |
| **4** | Barometer availability + drift | Nathan | ☐ | MINIMAL tier only; don't promise OPTIMISED live. **v2: re-verify on `expo-sensors`, not `sensors_plus`** |
| **5** | LLM tool-calling reliability ≥8/10 | Nathan | 🟡 | `qwen2.5:14b` (config.py's default) CUDA-crashed once on cold load, then passed cleanly on retry (204s) — this GPU has 8GB VRAM and `qwen2.5:14b` is a ~9GB Q4_K_M quant, likely too large to load reliably alongside other GPU load. **Tried swapping to `gemma2:9b` for VRAM safety in Phase A — reverted in Phase C: `gemma2:9b` does not support tool calling at all** (confirmed by a real `"does not support tools"` failure once the Overrun Council needed it). `qwen2.5:14b` stays the default; the VRAM risk is real but a non-tool-calling model is not a usable substitute. Re-run EXP-5's ≥8/10 tool-call count against `qwen2.5:14b` specifically before the demo |
| **6** | GPS accuracy under canopy | Nathan | ☐ | Wider block spacing in demo farm |
| **7** | ✅ CONFIRMED (v1 build) — CNN trains, 6 classes | Nathan | ✅ | Real v1 result: label-order bug found and fixed; `.onnx` now gives fast, accurate results |
| **8** | ✅ CONFIRMED (v1 build) — `.onnx` export works, accurate | Nathan | ✅ | **v2 note: `.tflite` export is no longer required** — ONNX is now sole primary, on-device tier is optional/deferred |
| **9** | Weather APIs + cached fallback | Abraham | ☐ | Ship cached data only |
| **10** | SRTM DEM sanity | Nathan | ☐ | Drop cross-check (it's optional anyway) |
| **11** | ⛔ OBSOLETE (v2) — scrcpy mirroring | Nathan | — | AICC-specific demo rig question; re-evaluate demo format for AgroHack's exhibition booth separately |
| **12** | ~25 Iban templates + audio clips | Abraham | ☐ | BM-only prompts |
| **13** | GCP deploy pipeline proven in sandbox | Nathan | ☐ | Stay local; GitHub repo + landing page still complete |
| **14** | `capture_target` vs predicted body part mismatch fires correctly | Nathan | ✅ | Verified via `sandbox/exp14_mismatch.py`, exercising the real `check_capture_mismatch` — all 10 cases behave as expected |
| **15** | 🔄 v2 NEW — Overrun Council's dose-less schema rejects a hallucinated treatment | Nathan | ✅ | Verified via `sandbox/exp15_council_wall.py` — `ValidationError` raised on a smuggled `treatment_id`/`dose_text_ms`. **Note for Phase C:** the real `TriageRanking` model must set `model_config = ConfigDict(extra="forbid")` — pydantic v2's default is to silently ignore extra fields, not raise, which would fail this exact check |
| **16** | 🔄 v2 NEW — MCP calendar sync & Google Calendar integration never fire without an approval check (Rule 13 Guardrails) | Nathan | ✅ | Fully validated across Phase 1, Phase 2, and Phase 3: OAuth 2.0 Web auth flow with Google API v3; Google Calendar MCP server (`backend/mcp_config.json`, `backend/app/mcp/google_calendar_server.py`) and client bridge (`backend/app/agent/mcp_calendar_client.py`); autonomous treatment schedule proposals (`CalendarEventProposal`) with Rule 13 Human-in-the-Loop approval gate; `TreatmentProposalCard` interactive component in Advisor screen (`frontend-rn/app/(tabs)/advisor.tsx`); verified via 15 unit tests (`test_calendar.py`, `test_mcp_calendar.py`) and `npx tsc --noEmit`. |
| **17** | 🔄 v2 NEW — terrain canvas rotation holds for a non-north-south farm | Nathan | ✅ | Verified via `backend/tests/test_terrain.py` — an east-west farm's flow vector still aligns to +Y post-rotation, and rotation is confirmed an isometry (real distances preserved) |

---

## Blocking items — resolve before frontend work begins

**EXP-3, 4, 5** are blocking for v2 (EXP-1's AICC-specific blocking status is retired; EXP-2 remains important but not launch-blocking since pre-recorded clips are the committed MVP regardless).

- **EXP-3 has no fallback.** If the phone cannot reach the laptop, the entire local-path demo fails. Solve this first — this did not change with the RN switch.
- **EXP-4 needs re-running on the new sensor library.** `expo-sensors`' barometer behaviour has not been verified — don't assume `sensors_plus`'s results carry over.
- **EXP-5 is the quiet killer.** A model that chats fine but won't emit valid tool calls will waste hours. Test it early, and re-test if the Overrun Council's 4-agent structure changes tool-calling load.

---

## Environment readiness

Tracked in `../WORKSPACE_SETUP.md`. **⚠️ v2: the Flutter/Gradle items below are retired — see `WORKSPACE_SETUP.md` for the Node/Expo/EAS replacement checklist.**

| Item | Status |
|---|---|
| `node --version` (v24.12.0) / `npx expo --version` (57.0.26) clean | ✅ |
| EAS CLI installed (24.7.0) — not yet on this shell's PATH, see note below | ✅ |
| Firebase CLI installed (15.28.1) — same PATH note | ✅ |
| **EAS build cache warmed** (throwaway `expo init` + `eas build`) | ☐ — do before Phase B needs a fast build |
| Docker base images pre-pulled | ☐ |
| Ollama models pulled (`qwen2.5:14b`, `gemma2:9b`, `gemma4:e2b`, `qwen2.5:1.5b`) | ✅ |
| HuggingFace models pre-downloaded | ☐ |
| Windows firewall: 8000, 8001, 11434 | ☐ — not verified from this session |
| `adb devices` sees the phone | ☐ — no phone attached in this session |
| APK signing keystore created (EAS-managed or local) | ☐ |
| GCP project + billing + APIs enabled | ☐ |
| `gcloud` CLI authenticated (SDK 574.0.0 present) | ☐ — auth not verified this session |
| Gemini API key issued and stored in `.env` | ☐ — `backend/.env` created this session from the CREDENTIALS.md template, key left blank for local Ollama path |

> **New note:** `eas`/`firebase` are installed under `%APPDATA%\npm` but that folder isn't on PATH in this Bash session (only `%APPDATA%\npm\bin`, which doesn't exist for npm's Windows layout) — check whether your interactive PowerShell/cmd PATH already has it; if not, add `%APPDATA%\npm` to PATH.

> ⚠️ **Warming the EAS build cache is the highest-value prep item**, same reasoning as v1's Gradle warning — a first cloud build can cost real time you don't want to lose mid-session.

---

## Assets ready for build day

| Asset | Owner | Status |
|---|---|---|
| **`.onnx` CNN weights + metrics — ✅ already produced in v1 build** | Nathan | ✅ |
| `healthy_leaf` and `healthy_collar` collected as independent classes, not derived from one bucket (`docs/L1_MODEL_ROADMAP.md` §1) | Nathan | ☐ |
| `templates.json` (~25, native-verified) | Abraham | ☐ |
| Pre-recorded `.wav` clip set | Abraham | ☐ |
| `rules.json` from MPB/DOA with citations | Abraham | ☐ |
| `weather_cache.json` fallback | Abraham | ☐ |
| Demo farm walk trace (GPS/baro) | Nathan | ☐ |

---

## Decisions still open

| # | Question | Default if undecided |
|---|---|---|
| 1 | ⛔ Retired (Flutter-only) — Riverpod vs Bloc | N/A — v2 uses Zustand + TanStack Query, not an open question |
| 2 | Demo farm: recorded real walk vs synthetic | **Recorded real walk** — proves the loop |
| 3 | ✅ Resolved — on-device vs backend inference | **Backend `.onnx`, confirmed sole primary.** On-device (`react-native-fast-tflite`) is optional/deferred, not a live decision |
| 4 | Speech: pre-recorded vs MMS live | **Pre-recorded** (MVP); MMS if EXP-2 passes |
| 5 | Read-only web dashboard for judges | Skip unless ahead of schedule |
| 6 | ✅ Resolved — cloud deployment gating | **No longer gated.** AgroHack's build-before-exhibition rule removes the time pressure that justified v1's "17:00 gate" — cloud is a first-class target now |
| 7 | ✅ Resolved — "vine" vs "block" terminology | **Confirmed "block."** Matches `blocks` as the central entity table; no schema change. See `docs/PROJECT_SPEC.md` §9.7 |
| 8 | ✅ Resolved — Settings screen placement | **Confirmed top-right icon**, not a 4th tab. Keeps the 3-tab (Home/Advisor/Farm) structure in §9.0 |
| 9 | 🔄 v2 NEW — `alerted` state time-based decay | **Undecided, deliberately.** No auto-clear rule exists yet for an `alerted` block with no confirming diagnosis after N days. See `docs/PROJECT_SPEC.md` §3 L4 block state model. Resolve before relying on long-idle demo data |

---

## Scope decisions

Features considered and explicitly set aside, with the reasoning — so they don't quietly resurface as "didn't we already plan this?"

| Feature | Status | Why |
|---|---|---|
| Object detection + per-leaf counting (L1) | **Set aside** | No downstream consumer of a prevalence count — block state, spread model and agent all react to *presence* of a class, not quantity. Requires hardware (elevated static cameras) to be workable at all, doesn't resolve leaf occlusion even then, and no annotated dataset exists. See `docs/L1_MODEL_ROADMAP.md` §1. |
| Farmer manually ranks leaf types by relative quantity | **Set aside** | Depends on the same prevalence signal nothing downstream consumes, and reintroduces a farmer judgment call the rest of the system is designed to remove. Superseded by multi-sample capture with worst-class-wins aggregation, which needs no ranking. See `docs/L1_MODEL_ROADMAP.md` §7.4. |
| Multi-sample capture (2–4 photos/block, worst-class-wins) | **Adopted — optional** | Additive to single-photo capture, not a replacement. Same model, same endpoint, no new training data. See `docs/L1_MODEL_ROADMAP.md` §7.4 and `docs/PROJECT_SPEC.md` §6. |

---

## Log

Record decisions that changed the plan. The **decision** matters more than the result.

| Date | EXP | Outcome | Change to plan |
|---|---|---|---|
| 2026-09-18 | 14/15/16 | All three pass — see `sandbox/exp14_mismatch.py`, `exp15_council_wall.py`, `exp16_calendar_gate.py` | None; EXP-15/16 double as a spec for Phase C — real `TriageRanking` must set `extra="forbid"` |
| 2026-09-18 | label-order check | `labels.txt` is alphabetical and passes the static checks in `sandbox/check_label_order.py`, but does NOT match `L1_IMPLEMENTATION_LOG.md`'s documented 0..5 index table, contradicting `docs/BUILD_LOG.md`'s claim that they match. No labelled images exist in-repo to verify empirically | Flagged to user, not resolved — needs the real training `class_to_idx` order confirmed before this is fully trusted |
| 2026-09-18 | EXP-5 (partial) | `qwen2.5:14b` (backend's config.py default) CUDA-crashed once cold-loading on an 8GB VRAM GPU, passed on retry (204s) | `backend/.env` created this session, initially defaulted `LITELLM_MODEL` to `ollama_chat/gemma2:9b` for VRAM safety |
| 2026-09-18 | EXP-5 (regression found + fixed) | Phase C: the Overrun Council's live test failed with `"gemma2:9b does not support tools"` -- confirmed via `ollama list`'s API that `gemma2:9b` lacks the `tools` capability entirely, unlike `qwen2.5:14b`/`qwen2.5:1.5b`/`gemma4:e2b` | Reverted `backend/.env` and `docs/CREDENTIALS.md` back to `qwen2.5:14b` as the local default; VRAM risk accepted as the lesser problem |
| 2026-09-18 | EXP-5 (flake found + narrowed) | `test_arbitration_defers_spray_and_prioritises_drainage_when_rain_is_imminent` failed twice live: `qwen2.5:14b` knew a treatment's real `rainfast_hours` (visible in its own `explain_why_flat` supporting_facts) but omitted that same value when calling `find_spray_window`, which treats a null `rainfast_hours` as "no wash-off constraint" and returns an immediately-viable window -- silently skipping the rainfast check it otherwise correctly reasoned about | Strengthened `find_spray_window`'s tool docstring (`backend/app/agent/tools.py`) to explicitly require passing through a non-null `rainfast_hours` from `get_treatment`. Confirmed via `git diff` this failure is unrelated to any Phase C code path (council/calendar/hybrid-search/drawer) -- the non-overrun arbitration control flow was untouched |
| 2026-09-18 | EXP-5 (real bug found + fixed) | A third live run of the same test hung ~18 minutes then failed with a captured warning: `find_spray_window`'s `forecast` argument failed FunctionTool validation (`ForecastPoint.date: Field required`) -- the model passed `get_weather`'s own `forecast_7d` items straight through, but `WeatherForecastOut` names that field `forecast_date` while `ForecastPoint` named it `date`. A genuine contract-drift bug (docs/CLAUDE.md § Conventions), not model flakiness | Renamed `ForecastPoint.date` -> `forecast_date` (`backend/app/schemas/treatment.py`, `backend/app/tools/treatment.py`, `backend/tests/test_treatment.py`) to match `WeatherForecastOut` exactly, so the model never needs to rename a field when chaining one tool's output into another's input. Full suite re-passed (44/44) |
| 2026-09-18 | EXP-9 (root cause confirmed + fixed) | Diagnosed precisely: the live data.gov.my API is healthy (confirmed via direct `curl`, `Tn187` station present with correct future dates) — it's not a provider problem, and not a request-window-size problem (a narrower ±3-day ask wouldn't fix a frozen historical dataset either). The real bug: `backend/seed/weather_cache_kuching.json` is a ONE-TIME static snapshot (2026-08-19 to 2026-08-25) that is only ever consulted when the live call fails, and once "today" moves past its 7-day window, **every** cached row lands in the past bucket, leaving `forecast_7d` permanently empty from then on | Added `_reanchor_dates_to_today()` (`backend/app/tools/weather.py`) — re-centers the cached snapshot's 7 dates symmetrically around the real "today" (≈3 days before/after, close to what was suggested) every time the fallback actually fires, never touching live data. Verified against the real cache file: now yields 4 real observations + 3 real forecasts regardless of how stale the underlying snapshot is. 4 new tests (`backend/tests/test_weather.py`), full suite 47/47 |
| 2026-09-18 | CNN path correction | Found a live `classifier_backend == "gemini"` branch still in `backend/app/routers/diagnosis.py`, contradicting the v2 rule that Gemini Vision is "removed entirely, not demoted" | Removed the branch; `diagnose_leaf` (`.onnx`) is now unconditionally the only path in `create_observation` |
| 2026-09-18 | Walk screen regression found + fixed | User caught it, not caught internally first: the v2 RN walk screen (Phase B) silently dropped three widgets the archived v1 Flutter build actually shipped (`flutter_app/lib/tier_banner.dart`, `flutter_app/lib/walk_map.dart`) — a live `WalkMap` (GPS track + numbered block pins + live position marker, OSM tiles), a live `AltitudeReadout`, and a `TierBanner` reporting barometer status. The `TierBanner` omission traced to a misread of `docs/PROJECT_SPEC.md` §9.10's "Silent — never shown as 'you have the lesser version'" line — the archived widget's own doc comment shows the real distinction is detection-silent, not result-hidden. This is exactly the kind of already-built feature PLAN.md's migration warning says not to silently drop | Corrected `docs/PROJECT_SPEC.md` §9.9/§9.10's data dictionaries to explicitly list `tier_banner`, `walk_map`, `altitude_readout`, and `position_readout` (a live lat/lon + distance-to-last-mark readout, a reasonable v2 extension of the same idea). Rebuilding `frontend-rn/app/(setup)/walk.tsx` and `register.tsx` against the corrected spec, using `expo-maps` (official Expo SDK package, Expo-Go-compatible) in place of v1's `flutter_map`+OSM, since a bare community map package would need a custom dev client |
| 2026-09-18 | Expo Go native-module incompatibility found + fixed (BLOCKING) | On-device test after adding the live map: `expo-maps` threw `Error: Cannot find native module 'ExpoMaps'`, and `TerrainScene.tsx`'s `@react-three/fiber`/`expo-gl` import (working moments earlier per the user's own report) also started throwing `TypeError: undefined is not a function` again — both at **module-import time**, not render time. That distinction mattered: expo-router's `useScreens.js` couldn't get a valid default export from `farm.tsx`/`validator.tsx`/`walk.tsx` (their imports of the crashing files never completed), which corrupted the whole Stack's screen registration and blocked the ENTIRE app at the splash/logo screen — not just the affected screens. A React error boundary cannot catch an import-time throw, only a render-time one | Split each risky component's prop types into a separate types-only file (`terrainTypes.ts`, `walkMapTypes.ts`) and added `SafeTerrainScene.tsx`/`SafeWalkMapView.tsx` wrappers that load the real implementation via a **guarded `require()`** (not a static `import`) inside try/catch — this defers the native-module lookup to a point the wrapper's own code controls, rather than Metro's unconditional module-evaluation hoist, so a missing module degrades to an informative fallback instead of crashing the bundle. `app/(tabs)/farm.tsx`, `app/(setup)/validator.tsx`, and `app/(setup)/walk.tsx` now import the Safe* wrappers. **Root fix still outstanding:** both `@react-three/fiber`+`expo-gl` (3D) and `expo-maps` (live map) need an EAS *development build* to actually render — Expo Go only runs the native modules Expo pre-bundled into its own binary, and neither of these is in it |
| 2026-09-18 | Media upload bug found + fixed (BLOCKING) | On-device: tapping "Save block" threw `Unsupported FormDataPart implementation`, blocking every block save (0 blocks could ever be captured). Root-caused via `node_modules` search, not guessed: `node_modules/expo/src/winter/fetch/convertFormData.ts` -- Expo SDK 57 overrides the global `fetch` with its own implementation, whose FormData converter only accepts a real `Blob` or an object with a `.bytes()` method. `frontend-rn/src/api/media.ts` was appending the classic React Native `{uri, type, name}` object literal (documented in `react-native/Libraries/Network/FormData.js` itself as the intended pattern) -- valid for RN's OWN raw `fetch`, not recognised by Expo's replacement. A second, independent bug in the same function: it manually set `Content-Type: multipart/form-data` with no `boundary` parameter, which a server cannot parse correctly on its own (fetch/FormData normally generates that boundary automatically) | Rewrote `uploadMedia()` to use `expo-file-system`'s native `UploadTask` (`UploadType.MULTIPART`, `fieldName: "file"`) instead of `fetch` + `FormData` -- bypasses both bugs entirely by not going through either FormData serialisation or manual header construction. Same backend contract (`POST /media`, field name `file`) |
| 2026-09-18 | Barometer -> risk math wiring confirmed (user asked directly) | Verified rather than asserted from memory: `app/tools/graph.py`'s `build_flow_edges` was completely untested for its barometer path (`grep` found zero references to `build_flow_edges` anywhere in `tests/`) -- risk existed that captured `baro_rel_m` readings were only ever used for elevation ranking, not the actual spread/risk calculation | Added `backend/tests/test_flow_edges.py` (4 tests, all pass) proving `baro_rel_m` drives a real `slope_ratio`/`elevation_drop_m` which feeds 40% of `flow_weight` (`W_SLOPE`) -- a steeper barometer-derived slope produces a strictly higher `flow_weight` than a gentle one (same distance/drainage held constant), missing/partial barometer data degrades to the documented neutral 0.5 slope component rather than crashing, and drainage still contributes independently. Confirms the answer to the user's question was already "yes" -- this closes the one real gap (test coverage) rather than changing any calculation logic |
| 2026-09-19 | Home priority card + missing Diagnosis Capture screen (found on-device) | (1) Home showed NO priority card until a recommendation existed, so a new farmer had no path to a first diagnosis. (2) The Advisor's "Begin Diagnosis" button routed to the Farm tab and stopped — reported as a possible Expo Go limitation, but it was **not**: the §9.8 Diagnosis Capture screen was never built (a Phase C note said it was deferred, and the placeholder route hid that). Without it nothing could ever create a recommendation, so the priority card could never populate for a real user. (3) Tab headers showed the page name instead of the brand logo | Priority card is now **always rendered**: empty state = "Start your first diagnosis" shortcut; populated = immediate action + **bento metric tiles** (replaces the tap-to-expand drawer) + "Diagnose again in Advisor" shortcut, which opens Advisor with `/diagnose` typed but **not sent**. Built `frontend-rn/app/diagnosis.tsx` (§9.8) over the existing cycle/observation/agent-run API; the exact request sequence was smoke-tested against a throwaway farm (mismatch → retake prompt; `force_accept` → counted, cycle → complete). Block Detail gained the spec'd "Diagnose in Advisor" button. Tab headers now show the real PepperDex Sarawak logo (transparent margins trimmed, artwork untouched). Spec §9.0/§9.2/§9.8 updated. **Open:** the agent run takes minutes on local `qwen2.5:14b` and the client just holds a spinner — needs a background job + polling; and on-device behaviour of all of this is not yet verified |
| 2026-09-20 | Stage 1 plan verified: local-backend standalone APK | Owner's plan (test with an EAS `.apk` against the laptop backend, no Metro; then ship) was checked against how Android/EAS actually behave and found sound with four gaps: (1) a release APK blocks plain `http://` (Android 9+), so a laptop-address APK could not reach the backend at all; (2) `frontend-rn/.env` is gitignored so EAS would have built with no API address; (3) the API address is baked at build time, so the test APK can never be the judges' APK — a second `cloud`-profile build is required; (4) each screen change costs a ~15-min, quota-limited build | Added `frontend-rn/app.config.js` (cleartext enabled **only** when `ALLOW_CLEARTEXT=1`, verified on for `local`, off for `cloud`/`production`) + `expo-build-properties`; added the `local` profile to `eas.json` carrying the LAN address; wrote the staged plan into `PLAN.md` § HOLD and the exact steps into `WORKSPACE_SETUP.md` § "Stage 1". **Not verified:** that an EAS build actually succeeds and that the installed APK reaches the backend on the phone — needs the owner's `eas login`/`eas build` |

| 2026-09-20 | Standalone `local` APK quit right after register → Continue (backend log ended at `GET /farms/<id>/dashboard 200`) | Cause (most likely, no logcat yet): walk screen mounts `expo-maps` GoogleMaps.View, which is compiled into a real build but has no Google Maps API key → native crash, uncatchable in JS. Fix: `SafeWalkMapView` only loads the map when `EXPO_PUBLIC_ENABLE_MAP=1` (off by default; fallback shows position/blocks). `eas.json`: `autoIncrement` on `local`/`cloud` so a rebuilt APK installs over the old one. **Needs on-device re-verification; if it still quits, capture `adb logcat -b crash`.** |

| 2026-09-20 | Round 2 of on-device findings (installed `local` APK) | (1) **Dashboard 500 after first diagnosis** -- `advisor.should_diagnose` subtracted a tz-naive SQLite `completed_at` from an aware `now_kuching()` → TypeError → app fell back to the first-diagnosis card. Fixed (normalise to Asia/Kuching); regression test `tests/test_settings_and_dashboard.py`. (2) **`POST /speech/render` 422** -- RainPulseCard sent slots `day`/`mm`; template needs `rainfall_mm`/`day_slot` (`seed/templates.json`). Fixed. PriorityActionCard sent empty slots (same 422 class) → now uses `/speech/say` with `reason_ms`. (3) **Rain-pulse values are NOT hardcoded**: `tools/weather.py` fetches data.gov.my forecast; mm is an *estimate* mapped from qualitative Malay text (`_SEVERITY_KEYWORDS`); falls back to the re-anchored cache snapshot with `is_cached_fallback=true` (shown in UI). (4) **Settings**: added `PATCH /users/{id}` (language) and `PATCH /farms/{id}` (rename), UI restored; added "Connected apps → Calendar" link/unlink (consent grant + device permission; events still approved one by one, rule 13). (5) **3D terrain**: switched import to `@react-three/fiber/native`, added render error boundary, load-error message + stack frame shown in the fallback. **Root cause NOT confirmed** -- needs the on-device message from the rebuilt APK. |

| 2026-09-20 | `POST /agent/run` hung 600 s (`litellm.Timeout` vs Ollama qwen2.5:14b) → no Recommendation rows → Home priority card stayed empty even with the dashboard 500 fixed | `runner.py`: LLM turn bounded by `AGENT_TIMEOUT_S` (default 180 s, `config.py`); on timeout or any LLM/transport error → `_deterministic_recommendations` (rules table only: `get_treatment` + `find_spray_window` + weather; clear_drain inserted before a rainfast-deferred spray; low-confidence/unknown → `inspect`; all healthy → `no_action`). Run is marked `status="partial"` and a `deterministic_fallback` entry is written to `tools_called` (never silent). Council failure no longer aborts the run. No dose/timing from any model (rule 2). Tests: `tests/test_agent_fallback.py`. **Not solved:** local qwen 14B is still slow; a full agent run only completes inside the timeout with a warm model or Gemini. |

| 2026-09-21 | Round 3 findings (APK v3) | (1) **Advisor card vanished** when urgency was `none` -- `index.tsx` hid it (`!== "none"`); now always shown ("Semua selamat"/"All clear"). (2) **Language never changed the UI** -- all strings hardcoded, mixed Malay/English. Added `src/i18n` (`useT()`, ms + en; **iba falls back to ms, stated in Settings, because no verified Iban text exists**). Converted: tabs, Home, Rain Pulse, Priority card, Advisor tab, Farm tab, Settings, Calendar modal. **Still hardcoded:** setup wizard, diagnosis capture flow, and backend-composed `reason_ms`. (3) **Calendar "Could not record consent"** -- real bug: `POST /calendar-grants` 500'd (`req.provider.value` on a plain str; schemas use `use_enum_values`). Fixed + roundtrip test. Note: this is a consent record + client-side `expo-calendar`; **there is no MCP server** (spec §9.15 defers Google Calendar MCP). (4) **3D terrain still fails on device** -- module loads fine in Node with native pkgs stubbed, so cause is phone-runtime specific and **unconfirmed**. `SafeTerrainScene` now loads three → expo-gl → r3f/native → TerrainScene one by one and the fallback panel prints `failed at [<layer>]: <message>`. Need that text from the next APK. |

| 2026-09-21 | Execute Roadmap Plan B (Vector DB, MCP, 3D Polyfill) | (1) **3D Terrain WebGL/three v0.186.0 crash**: Root caused to missing Hermes web globals (`TextEncoder`, `btoa`, etc.). Injected `src/polyfill.ts` into `app/_layout.tsx` using `fast-text-encoding`. (2) **Vector Database**: Replaced keyword overlap with true `sentence-transformers` embeddings (`all-MiniLM-L6-v2`) in `seed.py`. Refactored `retrieve_knowledge` to use cosine similarity via `numpy` + FTS5 boost. (3) **MCP Calendar**: Scaffolded a genuine `mcp_calendar_server.py` using `mcp.server.fastmcp` to replace the local device calendar bypass. (Pending Google Credentials). |
| 2026-09-22 | Architectural Verification: Seed vs. Vector DB vs. User Configs | Verified strict isolation between bootstrap seed data and real user state: (1) Vector DB (`knowledge_docs`) only indexes peer-reviewed agronomy literature via `all-MiniLM-L6-v2` + FTS5 — zero user configs or farm graphs are ever vectorized or retrieved from vectors. (2) User farm topologies, elevation ranks, and diagnoses are stored exclusively as relational SQL rows and queried dynamically via deterministic tools (`load_farm_graph`, `get_weather`, `compute_spread`, `query_farm_history`) filtered by `farm_id`. (3) `seed_demo_farm` mock records exist solely for offline developer testing under a dummy farm ID and never collide with or pollute real authenticated farmer sessions. |

| 2026-09-22 | Critical Issues 1-3 Deep Resolution | (1) 3D Terrain: Increased MAX_HEIGHT to 4.2 with high-contrast 6-stop elevation ramp and rock-tinted terrace cliff risers. Replaced single-pole marker with authentic VineClusterBlock (circular planting bed + 5 tiangs with climbing foliage + root disease core). Added 3D directional drainage flow vectors with downstream arrow cones and barrier indicators. Fixed drag trembling by decoupling delta-based PanResponder from useFrame momentum damping via isDraggingRef. (2) Calendar: Added try/except fallback in llm_tools.py draft_calendar_sync() to catch Ollama/CUDA crashes (exit status 0xc0000409) and return structured template draft. Clarified device calendar (expo-calendar auto-syncing to Google account) vs. backend MCP tool (authenticate_calendar.py) in settings and i18n. (3) Advisor Keyboard: Fixed Android KeyboardAvoidingView (behavior="padding", offset=60), added keyboardDidShow auto-scroll and keyboardShouldPersistTaps="handled". (4) Quality: npx tsc --noEmit compiles with 0 errors across frontend; backend tests (test_calendar, test_flow_edges, test_terrain) passed 13/13. |

| 2026-09-23 | (1) Bento tiles identical across diagnoses. (2) Calendar event "went through the API, not MCP". (3) No proposal cards after new diagnoses. (4) Minutes of blind spinner during agent runs | **(1) Two bugs, not one.** (a) `seed/seed.py` ran on *every startup* and backfilled ANY farm's recommendation lacking a risk row (real farms included) with constant 0.85 / 0.92 / 3 d / 45.2 mm -- invented numbers shown as spread-model output (rule 8). Removed; the 11 fabricated rows in the local DB were deleted (backup `backend/huluhilir.db.bak-2026-09-23`; identified by `source_block_id == block_id`, which compute_spread never produces). (b) `dashboard.py` picked the top action by `recommended_at` across ALL runs, so an old run (or an LLM-fabricated 2023 date) pinned the card. Now: first action of the most recent run; drawer falls back to the spread the source block projects downhill. Runner now always runs the deterministic `compute_spread` for diseased blocks the model didn't project, so every run has real metrics. **(2)** The MCP server itself calls the Google Calendar API (every Calendar MCP server does). But `approve` also silently fell back to a direct API write if MCP errored -- removed; MCP is now the only write path and failures surface. Client secret removed from `mcp_config.json` (was never committed; inherited from `backend/.env`). **(3)** qwen2.5:14b runs were marked `ok` while useless: one returned 0 recommendations, one said `no_action`/`inspect` for two collar-lesion blocks dated 2023-10-11, one called compute_spread with id `block-123` -> no treat action -> no proposal. Added `_guard_llm_recommendations` (drops unknown block/action/treatment, fixes stale dates, restores the rules-table action for untreated diseased blocks, logged as `rules_guard`); empty output now triggers the fallback. New diagnoses supersede un-acted pending proposals. **(4)** `POST /agent/run {background:true}` + `GET /agent/runs/{id}/events`; Advisor chat renders each real step as a message from a named agent (DiagnosisCoordinator, Spread Model, Rules Table, Advisor RAG, 4 council members, Calendar MCP, RootAgent), then the proposal cards. Tests: `tests/test_agent_feed.py` (3) + full suite 69 passed. **Not verified on device.** |

| 2026-09-24 | (1) Advisor had no farm memory ("does this mean the blocks are safer?" -> asked what crop). (2) Block states not updated after a new diagnosis | **(1)** `POST /advisor/ask` only ever received the question text. Added `app/tools/farm_context.py`: a DB snapshot per question (blocks + state, latest vs previous cycle diagnosis, latest action plan, projected spread, calendar proposals, rain, advisor verdict); request now takes `farm_id` + last 8 chat turns (the agents' decision included). Instruction rewritten (farm questions -> context; general -> RAG; may repeat the plan's action/block/date, never dose/product). Verified live on qwen2.5:14b, 35 s: correctly said the blocks are *not* safer (block 1's "healthy" was 43% low confidence, Block 2's photos unrelated). Also found: `observations.captured_at` arrives from the phone as UTC with no zone (reads 8 h early) -- context dates now use server-side cycle times; the stored column is **not yet fixed**. **(2)** Violated the spec's Block state model: states only escalated ("never downgrade"), and nothing set `alerted` from the spread projection. Now: first accepted photo of a block in a new cycle sets its state from that diagnosis (down too); worst-class-wins within a cycle; unrelated / <60% results are not evidence; runner raises protected -> alerted from `compute_spread` bands (never lowers). Tests: `tests/test_block_state.py` (2) + wet-week projection test; suite 72 passed. Existing "Harmed" states in the local DB were set by the old logic and will correct on the next cycle. |

| 2026-09-24 | New devices + Google linking (owner decision: build both recommendations) | **First launch:** new `app/(setup)/welcome.tsx` (bilingual) with "Try the demo farm" (`GET /demo-session` -> the seeded, set-up demo farm; shared by every device choosing it) or "Set up my farm" (normal wizard). **Google ownership:** one token per backend -> one owning farm, recorded at OAuth callback in `backend/calendar_owner.json` (gitignored; `CALENDAR_OWNER_FARM_ID` in `.env` pins it). Other farms: can't link (409), can't read or write the calendar (403 -- `GET /api/calendar/events` previously exposed the team's personal calendar to anyone), and approving a proposal records the approval in the app only (`status=approved`, no MCP call). The shared demo farm can never own the link. Owner-only `DELETE /api/calendar/google` to unlink. Owner set to Kebun MiaoMiao (the farm on the team phone). **Also found and fixed (rule 13):** the RootAgent had `schedule/reschedule/cancel_treatment_event` as tools, letting the model write to Google Calendar with no approval -- removed; the approval card is the only write path. Tests: `tests/test_calendar_owner.py` (3); old calendar tests updated (anonymous access now 403); suite 75 passed. |

| 2026-09-24 | Sync the v2 codebase to the "PepperDex" name; keep every credential/identifier | **Renamed:** all `huluhilir-rules` references in backend/app, tests, frontend-rn and the knowledge seed -> `pepperdex-rules` (the skill's real name); internal ADK names (`pepperdex`, `pepperdex_root`, `pepperdex_council` -- in-memory only); API/TTS titles; landing page text; README prose; demo farm row renamed in place at startup by `seed.py` ("Kebun Demo PepperDex", same farm_id -- local done, Cloud SQL on its next start). **Kept on purpose:** `huluhilir.db`, `huluhilir_l1*.onnx` + model_version, Cloud Run `huluhilir-api` (+URL, health `service` label), Cloud SQL `huluhilir-db`, secret `huluhilir-db-url`, bucket `huluhilir-media`, project `sfws-aicc-workspace-1`, repo `nathanyap17/huluhilir`, "(formerly HuluHilir)" notes, historical logs, v1 `flutter_app/`. **Doc bug fixed:** CLAUDE.md / CREDENTIALS.md / PLAN.md / WORKSPACE_SETUP.md told readers to deploy `pepperdex-api` into a new project `pepperdex-agrohack` -- that would have created a second service and a new URL. They now point at the real identifiers and `./deploy-cloud.sh`. Suite 75 passed; app + landing typecheck. |

| 2026-09-24 | (1) Photo times 8 h early. (2) Admin / demo / new farm / restore logic | **(1)** Not a phone bug: the app sends correct UTC (`toISOString()`), but SQLite drops the zone, so UTC and server Kuching times shared tables 8 h apart. New `KuchingDateTime` column type on all 33 timestamp columns (in: convert to Kuching; out: always aware). Local DB: 25 observations + 143 walk samples verified as UTC-stored (each checked against its diagnosis cycle / walk session window, 0 ambiguous) and shifted +8 h (backup `huluhilir.db.bak-2026-09-24-tz`); Cloud SQL unaffected (Postgres keeps zones). No region->timezone setting: all of Malaysia is UTC+8 and the phone already sends an absolute time. **(2)** Admin = farm_id pinned in `backend/.env` `CALENDAR_OWNER_FARM_ID` (Kebun MiaoMiao) -- keyed on id, so renaming is safe. Welcome screen: Try demo / Set up my farm / **Restore my farm**. Settings shows a rotatable restore code (`farm_restore_codes`); `POST /restore` returns the farm (wrong code -> one generic 404). Demo farm: no restore code, cannot be renamed, language changes stay on the device (`isDemo`). `GET /farms` now lists only the demo farm (v1 `/app/` still finds it) -- it used to expose every farm_id, which on this account-less API is the key to that farm, admin included. Tests: `test_restore.py` (3), UTC->Kuching test; suite 79 passed. |

| 2026-09-24 | `LANDING_PAGE.md` + `pitch-and-design/DECK.md` rewritten to the as-built v2 (APK v13) -- methodology, agent architecture, user flow | New: evidence table + deterministic safety checks, block-state rules, full agent tree (RootAgent, 2 loop agents, farm-memory Advisor, deterministic `/diagnose` verdict, 4-agent Overrun Council, approval-gated Google Calendar MCP), live agent feed, 9-step user flow (demo / setup / restore -> ... -> follow-up question), landing shortcuts table. **Overclaims removed or relabelled Roadmap:** DOA escalation with case file, extension-officer view, regional dataset, "edge" vision (it runs server-side), live walk map (off in this build), neighbour-alert approval (the agent drafts; no review screen exists in v2). 3D terrain marked ⚠ pending on-device check. |

| 2026-09-24 | APK on the owner's phone hotspot: farm name shown, Home blank, tab switches sent **no requests** to the backend | Laptop's address on the hotspot was `172.23.213.109`; the APK had `192.168.0.6` baked in, so every request went nowhere, and with no client timeout screens waited silently. Backend itself was fine (`0.0.0.0:8000`, answered on the new IP; firewall allows the venv Python on Public networks). Fix: server address is now a saved, runtime setting (`src/constants/config.ts` `apiBase()`; Settings -> Server address and the welcome screen, tested against `/health` before saving); typed API requests time out after 15 s (Advisor questions 200 s) so failures show "Could not load... check Settings -> Server address"; `eas.json` local default -> `172.23.213.109:8000`. Address parser unit-checked (7 cases); app typechecks and bundles. **Needs a new APK.** |

| 2026-09-24 | v14 round: (1) "Tool compute_spread rejected malformed input" on every run; (2) proposals scheduled at the current minute (3 pm -> 3-4 pm) or 00:00; (3) messages after proposal cards rendered above them; (4) Priority "Add to calendar" only opened the Advisor; (5) UI dull | **(1) Real error, 12/12 recent runs:** the tool asked the model for rain totals / disease class; qwen sent `-9999`, the literal text `"${weather.rainfall_7d}"`, and `"harmed_leaf"`. `compute_spread` now takes only `source_block_id`; class, rain and tier come from the DB/weather (same principle as the farm graph). Guards unchanged. **(2)** New `app/tools/scheduling.py` `workable_slot`: date-only -> 08:00, never sooner than the next full hour >=30 min ahead, field hours 07:00-17:00 else next 08:00; applied to schedulable recs before saving so card, decision message and proposal agree. The day still comes from the spray window (rule 2). **(3)** Proposal cards are now a chat message inserted at the "approval" event (and once on open for still-pending ones), not a pinned footer. **(4)** `POST /recommendations/{id}/calendar-proposal` returns the pending/approved proposal or drafts one with the same slot rule; the card's link passes the recommendation to the Advisor, which shows that card. Hidden for deferred actions. **(5)** Applied `pitch-and-design/MOCK_DESIGN.md` (never adopted before): paper/vine/water palette and the brief's state colours, Bricolage Grotesque + IBM Plex Sans/Mono (only 6 weights bundled), 20/16 dp radii, one soft shadow, spring press on buttons, spring expand on the rain chart; one flourish per screen -- water ripple (Home), leaf vein (Advisor), growing vine across setup (4 steps). Priority card: left border in the block's state colour, TTS button, mono metrics, visible "estimate" and "from the approved treatment table" tags. Consent buttons made equal weight (Approve had been 2x Reject's width -- violated A.15). **Deviations, forced by the brief's own contrast rule:** ink text on water fills (white was ~2.3:1); links in deeper water `#0B6680`. A.4's "/diagnose turns the input amber" not done -- it would reuse a state colour for UI, against §3. Tests: `test_scheduling.py` (9); suite 88 passed; app typechecks, expo-doctor 21/21, Android bundle builds. **Not seen on a device yet.** |

| 2026-09-24 | v14 test: (1) "Last check" tile over the estimate tag; header/nav should differ from content; (2) Spread Model / Rules Table messages twice; (3) phone's navigation bar covers the app's tab bar | **(1)** Metric tiles were forced square (`aspectRatio: 1`); a 3-line tile overflowed on narrow phones. Now `minHeight` and grow with content. Headers + tab bar white (`surface`) with a shadow line, content stays paper. **(2)** Not a double-send: the agent (correctly) calls get_treatment / compute_spread once per diseased block and each call was its own bubble. Consecutive plain steps from the same agent now merge into one message (`replaces` the previous seq; the app swaps it in place). Also found in the tool log: `find_spray_window` was fed a model-invented forecast dated **2023-09-01** -- it now takes only `treatment_id` and reads rain-fast hours from the rules table and the forecast from get_weather (same fix class as compute_spread). **(3) My regression from the redesign:** a fixed tab-bar `height: 64` dropped React Navigation's safe-area padding, so Android's nav bar sat over the tabs. Tab bar now `58 + insets.bottom`; root stack gives setup screens top+bottom insets and Settings / Diagnosis / Block detail bottom insets (SDK 57 is edge-to-edge). Tests: +2 (suite 90 passed); app typechecks and bundles. **Not seen on a device.** |

| 2026-09-24 | Owner: local testing **verified** -> HOLD lifted. Pre-ship changes | (1) Scratch files deleted (`plan_1166.md` held the Google client secret; none were gitignored); `backend/scratch_clean.py` (deleted future recommendations) removed. (2) `.dockerignore` excluded none of `backend/.env`, `token.json`, DB backups, `frontend-rn/` -- all would have been uploaded to Cloud Build and `.env`/token baked into the image; now excluded. (3) `deploy-cloud.sh`: classifier default was `gemini` (violates `.onnx`-only rule) -> `onnx`; `--no-cpu-throttling` (background agent runs), `--max-instances 1` (in-memory feed), 2 GiB, `AGENT_TIMEOUT_S=60`, Google OAuth + token from Secret Manager, owner pinned. (4) Dockerfile: CPU-only torch, embedding model baked in. (5) Google token path configurable (`TOKEN_FILE`, seeded from `GOOGLE_TOKEN_JSON`); MCP client lets the environment override `mcp_config.json`. (6) `backend/scripts/copy_farm_to_cloud.py`: copies one farm by id, FK-safe (farms<->walk_sessions cycle handled), idempotent, drops rows with broken references (found one: a recommendation for `block-123`); verified into an FK-enforcing SQLite target -- 159 rows, re-run 0, identical farm/blocks/restore code/history. (7) `eas.json` cloud profile -> `https://huluhilir-api-mfrzixfqeq-as.a.run.app`. Schema check vs deployed v1: only new tables, no new columns. Suite 90 passed. |

| 2026-09-24 | **Phase 1+2 shipped: v2 backend on Cloud Run, team farm copied** | Deployed revision `huluhilir-api-00042-thx` (rollback target `00041-5kh`). **Found:** (a) no `.gcloudignore` -> `gcloud run deploy --source` ignored `.dockerignore` and tried to upload `frontend-rn/node_modules` (and would have sent `.env`/`token.json`); added `.gcloudignore` (128 files uploaded, no secrets). (b) Service traffic was **pinned** to `00041`, so every deploy created a revision that was retired unserved; fixed with `update-traffic --to-latest` -- future deploys now roll forward. (c) Cloud SQL rejected the `huluhilir-db-url` password over the Auth Proxy (Cloud Run's own socket worked); owner chose to reset user `hulu` to the secret's value (no change for Cloud Run). Farm copy: 157 rows + 33 photos to `gs://huluhilir-media`, re-run inserts 0. Live checks: PepperDex API, demo farm renamed in Cloud SQL, `/farms` demo-only, calendar connected (Secret Manager token), MCP 5 tools, anon calendar read 403, farm dashboard/restore code/photo OK, Advisor on Vertex `gemini-2.5-flash` answered from farm memory in 19 s. Minor: `/api/calendar/mcp/status` reports `has_token:false` (checks the old path) -- cosmetic, fix next deploy. |
