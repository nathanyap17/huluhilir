# Copy to .env and fill in for a real device (see docs/CREDENTIALS.md §2).
# localhost resolves to the PHONE itself, not the laptop running the backend --
# use the laptop's LAN IP when testing over a hotspot (docs/PROJECT_SPEC.md §8).
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.23:8000
