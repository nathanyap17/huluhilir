fastapi
uvicorn[standard]
httpx
pydantic>=2
pydantic-settings
# [asyncio] + greenlet explicitly: newer SQLAlchemy no longer installs
# greenlet on its own, and a Cloud Run revision crashed on startup without it.
# Held on the 2.0 line the test suite runs on.
sqlalchemy[asyncio]>=2.0,<2.1
greenlet
aiosqlite
# CLOUD only: DATABASE_URL switches to postgresql+asyncpg on Cloud Run
# (Cloud Run's filesystem is ephemeral -- a SQLite file there is wiped on
# every new revision). No code branches on this; see app/db.py.
asyncpg
onnxruntime
pillow
numpy
litellm
google-adk
# Block E: agent output -> speech. ADC auth, no API key (see app/speech/synth.py).
google-cloud-texttospeech
python-multipart
pytest
pytest-asyncio
mcp
sentence-transformers
scikit-learn
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
